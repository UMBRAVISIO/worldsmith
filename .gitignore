# Large binary assets — splats are tens of MB each
worlds/*.ply
worlds/**/*.ply
site/worlds/*.ply

# Keep sample splats explicitly (override): !worlds/sample.ply

# Pipeline state / secrets (never commit)
.env
pipeline/.state/
*.ply.tmp

# OS noise
.DS_Store
Thumbs.db
