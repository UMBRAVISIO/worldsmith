# Large binary assets — full-res splats stay out of git (100-200MB)
# 100k/500k demo tiers ARE committed (needed for Pages deploys)
worlds/*_full.ply
worlds/*.spz
site/worlds/*_full.ply
site/worlds/*.spz

# Pipeline state / secrets (never commit)
.env
pipeline/.state/
*.ply.tmp

# OS noise
.DS_Store
Thumbs.db
